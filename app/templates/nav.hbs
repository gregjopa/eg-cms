<div class="navbar">
  <div class="navbar-inner">
    <ul class="nav">
      {{#each model}}
        <li>{{#linkToPageAnimated name this}} {{name}} {{/linkToPageAnimated}}</li>
      {{/each}}
    </ul>
  </div>
</div>
