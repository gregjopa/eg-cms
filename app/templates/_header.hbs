<header>
  {{#with data.config.fields}}
    <h1>
      <span class="logo">{{sitename}}</span>
      <span class="tagline">{{tagline}}</span>
    </h1>
  {{/with}}

  <!-- navigation -->
  {{render nav}}

</header>
